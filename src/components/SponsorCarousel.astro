---
// Determinar el idioma basado en la URL
const { pathname } = Astro.url;
const isSpanish = pathname.startsWith("/es/");

// Lista de patrocinadores principales
const sponsors = [
  {
    name: "Universidad Nacional de Colombia",
    logo: "/images/website/UNAL.png",
    url: "https://unal.edu.co/",
    logoClass: "unal-logo",
  },
  // Aquí se pueden añadir más patrocinadores en el futuro
];

// URL alternativa como respaldo
const alternativeLogoUrl =
  "https://unal.edu.co/typo3conf/ext/unal_skin_default/Resources/Public/images/escudoUnal.svg";
---

<!-- Sección de sponsors con diseño moderno -->
<section class="sponsors-section">
  <div class="sponsors-container">
    <div class="sponsors-content">
      <div class="sponsors-header">
        <span class="sponsors-badge">
          {isSpanish ? "🏛️ Afiliación" : "🏛️ Affiliation"}
        </span>
        <h2 class="sponsors-title">
          {
            isSpanish
              ? "Respaldados por Instituciones Líderes"
              : "Backed by Leading Institutions"
          }
        </h2>
      </div>
      
      <div class="sponsors-grid">
        {
          sponsors.map((sponsor) => (
            <a
              href={sponsor.url}
              target="_blank"
              rel="noopener noreferrer"
              class="sponsor-card glass"
              aria-label={`Visit ${sponsor.name} website`}
            >
              <div class="sponsor-logo-wrapper">
                <img
                  src={sponsor.logo}
                  alt={sponsor.name}
                  class="sponsor-logo"
                  width="200"
                  height="100"
                  loading="lazy"
                  onerror={`this.onerror=null; this.src='${alternativeLogoUrl}';`}
                />
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  /* ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     🏛️ SPONSORS SECTION
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

  .sponsors-section {
    position: relative;
    padding: var(--space-24) 0;
  }

  .sponsors-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .sponsors-content {
    text-align: center;
  }

  .sponsors-header {
    margin-bottom: var(--space-12);
  }

  .sponsors-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    color: var(--text-secondary);
    margin-bottom: var(--space-4);
    backdrop-filter: var(--glass-blur);
  }

  .sponsors-title {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--text-primary);
    margin: 0;
    letter-spacing: -0.02em;
  }

  .sponsors-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-6);
    margin-top: var(--space-8);
  }

  .sponsor-card {
    position: relative;
    padding: var(--space-8);
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    backdrop-filter: var(--glass-blur);
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 200px;
  }

  .sponsor-card:hover {
    transform: translateY(-4px);
    border-color: var(--primary-500);
    box-shadow: 0 8px 32px rgba(249, 115, 22, 0.2);
  }

  .sponsor-logo-wrapper {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .sponsor-logo {
    max-width: 100%;
    height: auto;
    max-height: 100px;
    object-fit: contain;
    filter: brightness(1.1);
    transition: all 0.3s ease;
  }

  .sponsor-card:hover .sponsor-logo {
    filter: brightness(1.2);
    transform: scale(1.05);
  }

  /* Modo claro - logo negro */
  :global(html.light-mode) .sponsor-logo,
  :global(body.light-mode) .sponsor-logo {
    filter: brightness(0);
  }

  :global(html.light-mode) .sponsor-card:hover .sponsor-logo,
  :global(body.light-mode) .sponsor-card:hover .sponsor-logo {
    filter: brightness(0);
    transform: scale(1.05);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .sponsors-section {
      padding: var(--space-16) 0;
    }

    .sponsors-container {
      padding: 0 var(--space-4);
    }

    .sponsors-title {
      font-size: var(--text-2xl);
    }

    .sponsors-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .sponsor-card {
      min-height: 180px;
      padding: var(--space-6);
    }

    .sponsor-logo {
      max-height: 80px;
    }
  }
</style>
