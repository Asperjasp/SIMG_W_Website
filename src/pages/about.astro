---
// Redireccionar a la versión en el idioma correcto
import { getLanguageFromURL } from "../utils/i18n.js";

const lang = getLanguageFromURL(Astro.url.pathname) || "en";
const destination = lang === "es" ? "/es/about" : "/en/about";
---

<meta http-equiv="refresh" content={`0;url=${destination}`} />

<script define:vars={{ destination }}>
  window.location.href = destination;
</script>

<div class="loading-container">
  <p>Redirecting to {destination}</p>
</div>

<style>
  .loading-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    font-family: Arial, sans-serif;
    color: #333;
  }
</style>
