// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://simg-seminar.com', // Reemplaza con tu dominio real
  integrations: [
    // Añade aquí cualquier integración que estés usando
  ]
});
